---
import LowercaseRedirect from "../../components/LowercaseRedirect.astro";
import { toStatePath, withBase } from "../../lib/routing";

const baseUrl = import.meta.env.BASE_URL;
---

<LowercaseRedirect
  to={withBase(baseUrl, toStatePath("TN"))}
  title="Redirecting to /state/tn"
/>
